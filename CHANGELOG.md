# 变更记录

## 1.5.0

- 打包产物默认文件名改为英文：PyInstaller `--name` 由「哈希工具」改为 `HashTool`，产物为 `dist\HashTool.exe`（build.bat 与 README 同步更新）
- 修复：计算中关闭对比窗口或切换语言后，三个对比标签页的轮询可能对已销毁控件抛 TclError——`_poll` 入口统一先检查 `winfo_exists()`
- 修复：切换语言时只取消了单文件校验的后台线程，两文件互比/批量比对会白算并向旧界面发消息，现与 `_on_destroy` 一致统一取消
- 修复：后台 worker 增加 try/finally 兜底，任何意外异常都保证发送 done（此前会让界面永久卡在「计算中」）
- 修复：添加文件夹时目录递归遍历移到后台线程（大目录不再冻结界面）；斑马纹改用文件序号计算，消除逐行 `get_children()` 的 O(n²) 开销
- 修复：剪贴板被其他程序占用时复制抛 TclError，现提示「复制失败」
- 修复：计算中切换语言丢失「计算中…」行状态，界面重建后按进度核算补回
- 修复：四处硬编码 10 元素空行元组抽为 `_empty_row`，列数随算法列表自动适配
- 修复：导出对话框防重入（连按 Ctrl+S 不再叠窗）；计算中右键「从列表移除」置灰（此前静默无效）
- 清理：`read_text_file` 移除无效的第三次 UTF-8 尝试
- 配置目录英文化：`%APPDATA%\哈希工具` → `%APPDATA%\HashTool`，启动时自动迁移旧配置并清理空目录
- 开发：引入 ruff 静态检查（零配置，`python -m ruff check`）
- 修复：目录扫描窗口期的边缘防御——扫描进行中（含结果已得出、待轮询回插的窗口期）禁止清空列表/移除文件/重复添加/开始计算；扫描结果队列改为局部引用随轮询传递，避免窗口期内连续扫描顶掉引用丢失结果
- 文档：新增英文版 README（README_EN.md，与中文版互相链接）
- 发布：开源至 GitHub（MIT 协议），Release v1.5.0 附带 exe 与 SHA-256 校验和
- 展示：README 徽章（许可证/CI/Release/Python）与中英文界面截图；新增 GitHub Actions（push/PR 自动跑 pytest + ruff，Python 3.11/3.13 矩阵）

## 1.4.1

- 修复：切换语言后「并行线程数」下拉框残留旧语言的“自动”文案（且不在新值列表中），现按新语言重建，数字选项原样保留
- 修复：批量比对清单选择对话框的文件过滤器误用“全选/All”文案，新增“清单文件/Hash List”与“所有文件/All Files”专用词条
- 修复：两文件互比取消时英文模式输出混入中文句号（`Cancelled。`）
- 修复：打开资源管理器失败提示误用“导出失败”文案，改为专用词条；单文件校验/两文件互比的浏览对话框标题统一为“选择文件/Select File”
- 新增下拉框语言切换的 GUI 回归测试
- 项目目录更名为 `HashTool`（英文路径）

## 1.4.0

- 新增中英双语界面：菜单「语言 / Language」即时切换，主窗口、对比窗口、状态提示、导出表头全部同步切换
- 新增 `i18n.py`：中/英字符串表 + `tr()`、系统语言自动检测（首次启动）、选择持久化到 `%APPDATA%\哈希工具\config.json`
- `hash_core.py` 的错误提示与导出表头国际化（语言随当前设置）
- 语言切换时保留文件列表、计算结果与各标签页输入（界面就地重建恢复）
- 新增 `tests/test_i18n.py`（双语完整性、占位符一致性、英文模式核心行为、配置读写）与 GUI 语言切换冒烟测试

## 1.3.1

- 修复：「并行线程数」下拉框在计算期间禁用时无视觉反馈——TCombobox 样式表 disabled 规则被 readonly 遮蔽（style map 首个匹配生效），调整顺序后禁用显示为灰底灰字
- benchmark.py 输出强制 UTF-8，修复经管道/重定向运行时中文乱码
- 进度核算抽为 `hash_core.ProgressTracker` 纯逻辑类，并补充乱序消息序列单元测试（模拟并行结果乱序到达，验证进度单调性与收敛性）

## 1.3.0

- 界面重设计为包豪斯风格：暖纸底色、墨黑标题带（红圆/黄三角/蓝方块几何标识）、扁平化按钮（主操作实心蓝、取消实心红）、墨黑表头、蓝色进度条与选中色、菜单与弹窗统一配色
- 图标更换为包豪斯三原色几何图形（make_icon.py 重写绘制逻辑）
- 计算期间禁用「并行线程数」下拉框，避免误以为即时生效
- benchmark.py 输出统一为 f-string 风格

## 1.2.0

- 新增「并行线程数」下拉配置（自动 / 1 / 2 / 4），计算线程数可调整
- 线程数解析抽为 `HashToolApp._resolve_worker_count`，并补充对应单元测试
- 新增 `benchmark.py` 并行基准脚本，README 附本机实测加速数据
- 新增本变更记录（CHANGELOG.md）

## 1.1.0

- 多文件并行计算：后台线程池最多 4 线程同时计算（hashlib 大块计算会释放 GIL）
- 8MB 分块读取改为预分配缓冲区 + `readinto`，减少大文件计算时的内存抖动
- 界面美化：表格斑马纹、微软雅黑字体、窗口图标（打包后取 exe 自身图标）、哈希值等宽字体显示
- 修复：右键点击行尾空白列可能出错、两文件互比计算期间改动算法勾选导致显示错乱、0 字节成功文件大小显示为空
- 新增 `tests/test_app_worker.py` 工作线程单元测试（无需图形环境）

## 1.0.0

- 首次发布：MD5 / SHA-1 / SHA-256 / SHA-512 / CRC32 哈希计算（8MB 分块、取消与进度）
- 三种对比模式：单文件校验（算法自动识别）、两文件互比、批量比对（MD5Sum/SHA256SUM 清单）
- 文件添加：对话框多选、拖拽文件/文件夹、整文件夹递归
- 结果导出：CSV（BOM）与标准 SUM 格式 TXT；双击/右键复制哈希
- Windows 原生拖拽（WM_DROPFILES，零第三方依赖）；PyInstaller 单文件打包
